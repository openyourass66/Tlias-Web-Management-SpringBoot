<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhang.Mapper.StudentMapper">
<!--    分页条件查询-->
    <select id="page" resultType="com.zhang.pojo.Student">
        select s.*,c.name clazzName from student s left join clazz c on s.classId=c.id
        <where>
            <if test="name!=null and name!=''">
                s.name like concat('%',#{name},'%')
            </if>
            <if test="degree!=null">
                and s.degree=#{degree}
            </if>
            <if test="classId!=null">
                and s.classId=#{classId}
            </if>
        </where>
        order by s.updateTime desc
    </select>
<!--    根据id查询-->
    <select id="findById">
        select * from student where id=#{id}
    </select>
<!--   添加学生-->
    <insert id="add">
    insert into student(name,no,gender,phone,idCard,isCollege,address,degree,graduationDate,clazzId,createTime,updateTime)
    values(#{name},#{no},#{gender},#{phone},#{idCard},#{isCollege},#{address},#{degree},#{graduationDate},#{clazzId},#{createTime},#{updateTime})
    </insert>
<!--    修改学生-->
    <update id="update">
        update student
        <set>
            <if test="name!=null">
                name=#{name},
            </if>
            <if test="no!=null">
                no=#{no},
            </if>
            <if test="gender!=null">
                gender=#{gender},
            </if>
            <if test="phone!=null">
                phone=#{phone},
            </if>
            <if test="idCard!=null">
                idCard=#{idCard},
            </if>
            <if test="isCollege!=null">
                isCollege=#{isCollege},
            </if>
            <if test="address!=null">
                address=#{address},
            </if>
            <if test="degree!=null">
                degree=#{degree},
            </if>
            <if test="graduationDate!=null">
                graduationDate=#{graduationDate},
            </if>
            <if test="clazzId!=null">
                clazzId=#{clazzId},
            </if>
            <if test="violationCount!=null">
                violationCount=#{violationCount},
            </if>
            <if test="violationScore!=null">
                violationScore=#{violationScore},
            </if>
        </set>
        where id=#{id}
    </update>
<!--   删除学生-->
    <delete id="deleteByIds">
        delete from student where id in
        <foreach item="id" collection="ids" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>
<!--    违纪处理-->
    <update id="violation">
        update student
        set violationCount=violationCount+1,violationScore=violationScore+#{score}
        where id=#{id}
    </update>
<!--    统计学员学历-->
    <select id="getStudentDegreeData" resultType="com.zhang.pojo.DegreeOption">
        select
          (case when degree=1 then '初中'
                when degree=2 then '高中'
                when degree=3 then '大专'
                when degree=4 then '本科'
                when degree=5 then '硕士'
                when degree=6 then '博士'
                else '其他' end) degree,
          count(*) num
        from student
        group by degree order by num
    </select>
</mapper>